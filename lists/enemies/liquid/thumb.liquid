{% assign props = __item__.props %}

{% if props.thumb %}
	{% assign thumb = props.thumb %}
{% else %}
	{% assign thumb = props.img %}
{% endif %}

{
	"src": {{thumb.src}},
	"width": {{thumb.width | times: 3}},
	"height": {{thumb.height | times: 3}},
	"pixelated": true
}
